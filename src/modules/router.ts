import Router from 'koa-router'
export const router = new Router()

// EXAMPLE
// router.get('/get/url', async (ctx, next) => {
//   const { id } = ctx.params
//   ctx.body = {
//     contract: _contract
//   }
//   await next()
// })
